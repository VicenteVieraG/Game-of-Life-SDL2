ADD_LIBRARY(${sdl2} SHARED IMPORTED GLOBAL)

SET_TARGET_PROPERTIES(${sdl2} PROPERTIES
    IMPORTED_LOCATION ${SDL2_PATH}
    IMPORTED_IMPLIB ${SDL2_IMPORT_PATH}
    INTERFACE_INCLUDE_DIRECTORIES ${SDL2_INCLUDE_DIR}
)